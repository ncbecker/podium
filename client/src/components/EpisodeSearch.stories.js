import { EpisodeSearch } from "./EpisodeSearch";

export default {
  title: "Podium/Input",
  parameters: { layout: "centered" },
  component: EpisodeSearch,
};

export const Search = () => <EpisodeSearch />;
